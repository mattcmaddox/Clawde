//! Shared test support for `clawde-api` integration tests.
//!
//! Files under `tests/common/` are not compiled as standalone test binaries;
//! test files import them with `mod common;`.

pub mod mock_provider;
