[package]
name = "clawde-buddy"
version.workspace = true
description = "Complementary helper crate for the bridge protocol and session management"
edition.workspace = true

[dependencies]
clawde-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
hex = { workspace = true }
chrono = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
